# V4: the expressive Material 3 conversion

The design baseline and the order of work. Authority over `DESIGN.md`, which
predates the overhaul: where they disagree, this file wins and the `DESIGN.md`
section is marked superseded. D172, D173, D174, D175.

**Phase one is conformance: every element in the v4 language, nothing invented.**
**Phase two is polish, and only after phase one is done.** Animation, motion and
small touches. Not before: polish on an unconverted component is work thrown
away.

## 1. The baseline

`docs/screenshots/m3v4-{0..5}-light.png`, approved by the owner. **Open them.
Do not build from memory of them.** Three rounds were lost that way, D172.

What they establish:

- **One saturated block per screen, everything else quiet.** The lead is the
  section base with white type on it. Cards under it are white with a small hue
  dot. Six tinted cards of equal weight is a rainbow and gives the eye nowhere
  to land.
- **Restraint around the one colored moment is what keeps it from going dead.**
  Not more color.
- **Gold is an accent**: a chip, an icon, the capture button. Never a field.
- **Tiles are tiles**, 120 against 170. Not squares.
- **Containers, not loose rows.** Related fields sit on one surface at
  `Radius.cardLarge`. A label, a box and a gap repeated down a page is rule 15's
  uniform weight wearing a form.

## 1a. What is still the old language, 2026-08-16

**The owner, looking at the build after the materials pass: "this is like 60
percent the old design ... old headers, old page titles, old accordions, old
buttons, old projects, and the list goes on."** He is right, and the reason the
last pass did not find it is written in D170 already: **a token layer cannot
produce an arrangement.** The materials were converted and the structure
underneath them was not touched, which is new paint on the old structure, which
is the exact sentence D170 uses.

**`check_v4.py` reporting zero does not mean this is done.** It measures four
mechanical tells and is blind to every item below. It was reported as the
overhaul being finished and that was wrong.

Against D167's own list of what the language is:

| D167 says | What is built |
|---|---|
| vibrant tonal containers | white slabs with hairline rules between rows |
| the shape library for variety, not one radius | **one radius**, and the v4 pass made this worse by deleting the 22dp corner and moving 58 surfaces to 26dp |
| emphasized editorial type, hard weight contrast | display sizes are right; screens still lead with a small title over a gray sentence |
| short navigation bars | done |
| springs, morphing capture button | done |

**The specific old-language elements to remove**, all of which appear on nearly
every screen:

1. **The hairline-divided monolith.** A group of rows is one white card with
   thin rules between items. That is a 2014 list. Expressive separates items
   into their own tonal containers with air between them.
2. **The outlined pill button.** `QuietButton` and `TextAction` are thin
   outlines. Expressive actions are filled tonal with a corner of their own,
   which `Radius.button` now exists for and nothing uses yet.
3. **The full width "Back to the notebook" footer** on every section screen.
4. **The sand accordion.** `FoldRow` is the "copy and paste from the old
   design" the owner named on the care team screen in D173, and it is still the
   same object with a new radius.
5. **The section header**: a small chip, a `displayM` title and a gray
   sentence, on every interior screen. No screen but Today leads with anything.
6. **Projects** end to end.

**The order that follows from D167: system layer first, then arrangements.**
The container and the action are shared components, so tonal containers and
filled actions reach every screen at once. Only after that does per-screen
arrangement work make sense, because arranging old components just moves them.

## 1b. The rebuild is a replacement, and the foundation is already here

**The owner, 2026-08-16:** "at the foundation of the design is using Google's
Material 3 Expressive design elements and assets. from there we enhance it.
we're not tweaking what we have now or updating what the old code is. we are
replacing completely. that means all of the old user interface disappears and
is gone and is written from the ground up."

**This supersedes the app's own-vocabulary habit.** Until now the app imported
almost nothing from Material beyond `Text` and drew its own switch, its own
chevron, its own buttons. That was defensible and it is not what is wanted:
Google's components are the foundation and this app's character is applied on
top of them.

**Verified on the classpath rather than assumed**, because the whole plan rests
on it. `compose-bom 2026.06.01` resolves `androidx.compose.material3:material3`
to **1.4.0**, and that artifact contains:

- `MaterialExpressiveTheme` and `ExperimentalMaterial3ExpressiveApi`
- `MotionScheme`, including the expressive motion scheme
- component tokens for `ButtonGroup`, `ConnectedButtonGroup`, `SplitButton`,
  `FloatingToolbar` and `LoadingIndicator`
- `ShapeDefaults`

**So no dependency change is needed to start.** The Expressive foundation has
been available the whole time and the app has not used one piece of it. The app
is still wrapped in `MaterialTheme` rather than `MaterialExpressiveTheme`.

**`MaterialShapes`, the 35 shape library D167 names, is not in this artifact**
and comes from `androidx.graphics:graphics-shapes`. That is the one dependency
the shape variety work will need, and it should be confirmed against the
current release before being added.

**The order.** Theme first, because `MaterialExpressiveTheme` changes the
shape, color and motion defaults every component below it reads. Then the
shared containers and actions, because those reach every screen. Then screens.
**Nothing is tweaked on the way**: a screen is rewritten or it is left alone
until its turn, so the build never carries half of each language at once, which
is the state the owner is looking at now.

## 2. Conformance, which is a command

```
python3 tools/checks/check_v4.py
```

Prints how many components and screens are converted and what remains. It fails
if a converted file regresses, or if something on a remaining list is actually
done. **Both `REMAINING` sets in that file only ever shrink.**

**It covers screens as well as components, and for its first day it did not.**
It read 55 component files and was blind to the 85 screens that arrange them,
which is most of what the person sees. The overhaul being three components deep
was caught by the owner's eye rather than by the command whose job that is.

**A screen's press tell is not a component's.** Passing `onClick` into a
converted component is correct: the spring lives one level down. Only a raw
`Modifier.clickable` written on the screen is silence. Counting the component
way reported 51 offending screens when there was one.

The four tells it looks for, each found on the phone:

| Tell | Meaning | The fix |
|---|---|---|
| `no-press` | tappable and silent under the finger, rule 16 | spring via `pressScale` or `openableByTap` |
| `ripple` | the other app's interaction grammar | `indication = null` plus the spring |
| `mono` | typewriter face on a word | `type.bodyS`. Mono is for figures in a column |
| `old-shape` | `Radius.card`/`fold`/`small` | `Radius.cardLarge`, or `hero` for a lead |

`ALLOWED` holds the components that keep a tell on purpose, with the reason.

## 3. Type

Two faces. **Atkinson Hyperlegible** for everything a person reads, at Bold for
display. **JetBrains Mono** for figures that line up in a column only: chart
axes, day grids, reference values, the date picker, the scrubber, a step number.

Bricolage Grotesque was removed in D174. The owner, three times: the fonts are
unnatural, they do not mix, "that font at the top is just weird". A display face
whose appeal is its quirks does not pair with a legibility face.

A bundled display face with more elegance and no quirk, in the Inter or Plus
Jakarta Sans family, is worth revisiting when there is network to fetch one.

## 4. The order of work

Component before screen. **A component fixed once is fixed on every screen that
uses it**, and screen-by-screen is what produced an overhaul three components
deep.

Seven layers, in order. Each one is finished before the next starts, because
every one of them is cheaper to do once the layer under it stopped moving.

0. **Conformance covers the whole app**, so "is it done" stays a command rather
   than an opinion. Done.
1. **The fourteen components** the check names. Batch them by tell, not by
   screen: all `no-press` together, then all `old-shape`.
2. **The thirty-five screens' materials.** Mechanical and defined by the check:
   44 `Radius.card` sites, the mono sites judged word against figure, one raw
   M3 card, one genuinely silent surface.
3. **The screens that arrange them.** Not restyling, which layers 1 and 2 now
   carry, but hierarchy: what leads, what recedes, what is grouped. `#381` is
   the forms; Projects home, the Document screen, Trail, Progress, Money,
   Chapters and Threads have not been read against section 1.
4. **The 85 screens' states**, per rule 11: empty, one, many, long text, RTL.
5. **Then polish.** `#383`.
6. **The gate**: `run_all.py`, `verify.sh`, the full instrumented suite.
7. **The phone**, per rule 21, and the screenshots the owner approves from.

**Layers 2 and 3 are different jobs and doing them together is the trap.**
Layer 2 is a radius and a typeface, findable by grep and provably finished.
Layer 3 is judgment about what leads, and it cannot be checked by a command.
Mixing them is how a screen gets declared done because its corners are right.

## 5. Rules that bite here

- 11: no truncation, no blank areas, every state built.
- 14: the bar is retroactive. When it rises, everything already built comes up.
- 15: hierarchy before decoration.
- 16: everything the person touches responds.
- 21: look at it on the phone before closing anything.
- 22: pick the component from the shape of the content. Not everything is a card.

## 6. Working notes

- **Read the last line of `tools/seed.sh` before trusting a screenshot.** It
  says "Restored." or "The restore did not finish". A failed seed leaves an
  empty notebook and every capture afterward shows empty states. `docs/TRAPS.md` 1.
- **Do not compile while the instrumented suite runs.** It recompiles and picks
  up half-written files.
- Screenshots: `tools/screenshot.sh <name> light`. Device helpers assume
  `ADB=/home/Kamsiob/Android/Sdk/platform-tools/adb`.
