WALK1 | critical | cannot-correct | An entry filed to the wrong care thread can never be moved. fileEntry only INSERTs into entry_thread (Repository.kt:1491-1505); no DELETE or UPDATE on that table exists anywhere, and is_unfiled is never set back to 1. EntryScreen.kt:249-278 shows the thread as a read-only door. Only remedy: delete and retype, losing people, project, incident and chapter links, exactly the loss CorrectEntryScreen.kt:57-61 was written to prevent.
WALK1 | critical | cannot-correct | Two care-team rows that turn out to be one person cannot be merged. No merge function exists; entry_person has no UPDATE or DELETE writer (linkEntryToPerson, Repository.kt:5390, is capture-time only). Archiving keeps a duplicate, deleting orphans the history.
WALK1 | medium | cannot-correct | A medication event's date is uncorrectable: insert-only writer at Repository.kt:5960, no updateMedicationEvent. Rule 17's "editable forever" holds for entry rows only.
WALK1 | low | wrong-place | Every correction lives on the thing's detail screen, never the list where the mistake is spotted (CareTeamScreen.kt:97-101 has no edit). Name fix 3 taps, entry date 4, medication delete 3. All work.
WALK2 | high | cannot-retrieve | No handover artifact exists. Exactly five things leave the app (Share.kt:75,137): one incident, the emergency card, one prep sheet, one month review, one document image. Nothing shares the care-team roster, standing instructions, open incidents as a set, pending questions as a set, or projects with who-has-what.
WALK2 | high | cannot-retrieve | The readable HTML is 40 per-table dumps (ReadableArchive.kt:191-227). Complete but schema-shaped: no open/pending grouping, no cross-table narrative. Attachments ship as bare hex filenames with no extension (Attachments.kt:93), so a browser downloads rather than displays them.
WALK2 | medium | dead-end | The merge report is thrown away: RestoreFlow.kt:161 does .map { 0 }, discarding MergeApply's inserted/updated/skipped/conflict counts. The phone says only "Done. Both notebooks are here now." Skipped rows are silent.
WALK2 | medium | wrong-place | A newer archive on an older build trips unknownShape and is reported as tampering, not version skew (ExportContainer.kt:1390-1406).
WALK3 | critical | cannot-retrieve | resolution_note has no writer in shipped code. resolveIncident (Repository.kt:5154) has one caller, NotebookShell.kt:1304, which never passes it. The Resolution block (IncidentScreen.kt:534, Readable.kt:87) is unreachable. Worse: Repository.kt:5163-5167 writes resolution_note = ? unconditionally with a null default, so reopening erases any note that existed.
WALK3 | high | dead-end | Once every incident is resolved the incidents list has no door. Notebook.kt:295 gates the card on waiting > 0 || openIncidents > 0; the count is open-only (Repository.kt:7900); More has no incidents row.
WALK3 | high | cannot-retrieve | Search does not index incidents or care threads. Repository.search covers ten tables (:4742-4767); live_incident and live_care_thread are not among them.
WALK3 | high | missing-link | Nothing can say "this is the old thing returning". The generic link table (schema.sql:1326) has two writers, both hardcoded: entry to project, and memo to its host. No entry-to-entry, no incident-to-incident, no picker. NoteScreen.kt:152 renders the target as a label, not a control.
WALK3 | medium | dead-end | A care thread can never be ended, therefore never reopened. ended_edtf/end_note exist (schema.sql:277-281) and CareThreadsScreen.kt:99 splits running from ended, but no Kotlin writer exists. Dead schema.
WALK4 | high | missing-link | An already-saved entry, document-derived evidence or appointment cannot be attached to a project after the fact. linkEntryToProject (Repository.kt:6927) has four call sites, all at entry-creation time. Existing documents can be filed (ChoosePaperScreen.kt:51-121); entries and appointments cannot.
WALK4 | high | cannot-file | "Who has it" cannot name a care team member. addProjectStanding accepts personId/organizationId (:6621) but the only caller passes neither (NotebookShell.kt:1599-1608). StandingSheet.kt:150 is free text and its chips are prior labels on this project, not the roster. project_person has no writer at all.
WALK4 | high | cannot-file | No outcome. Status is a closed set of five (schema.sql:993): active, waiting, stalled, done, abandoned. Won, denied or withdrawn is not representable; a lost appeal closes as "Done".
WALK4 | medium | wrong-place | The deadline never reaches Today by default. project_date/project_standing are hand-add-only cards (AddCardSheet.kt:248) and none of the 14 shipped starting hands contains a project card.
WALK4 | medium | cannot-file | 8 taps to record "the insurer has had it since the 12th", and it records who has it, not that anything was sent: project_paper.direction has no writer (NotebookOverlays.kt:1697 passes null).
WALK5 | critical | wrong-place | The father is created with a name and nothing else. addSubject (Repository.kt:256) calls createSubject plus makeSubjectActive; the first-run path (AppRoot.kt:194-273) also creates a chapter, a contact and a situation. So he gets no care threads, no chapter, no Today layout, lands on a different Today screen (NotebookShell.kt:711) that cannot be arranged, and nothing of his ever reaches the unfiled tray (NotebookShell.kt:3065 requires threads.isNotEmpty()). The copy promises "their own care team, medications, papers and trail" (en.json:1657). Recovery exists but is undiscoverable.
WALK5 | critical | cannot-retrieve | Whose record is on screen is answered on Today alone. subjectName appears in three files, all Today. Trail, Search, Notebook, Projects, Memos, Care Team, Documents, every detail screen and the capture form show nothing. The gold button sits on every destination and the capture form has zero mention of the subject, so filing the father's incident onto the mother is invisible at the moment it happens.
WALK5 | high | wrong-place | The Today switcher is on the wrong Today. TodayScreen.kt:455 has the avatar door (2 taps); TodayField.kt has none. TodayField is chosen whenever a layout exists, so the year-old mother's notebook has no switcher and the brand-new father's does.
WALK5 | medium | cannot-correct | makeSubjectActive (Repository.kt:236-247) is two unwrapped writes despite its own comment claiming one transaction, and schema.sql:210 has no unique index on is_active. activeSubject() breaks ties with ORDER BY created_at LIMIT 1, silently preferring the mother. Every add opens a transient two-active window.
WALK5 | medium | tap-cost | 3 taps to switch, every time, and More carries two adjacent same-hue rows both about "the person".
WALK5 | low | wrong-place | Two things leak across subjects: ownTemplates (Repository.kt:3090, custom_template has no subject_id) and organizationNamed (:2153). The readable export also interleaves both parents per table page and joins both names in the index headline (ReadableRows.kt:112-117).
WORKED-WELL | The keep-both merge is the strongest thing here: id-matched, updated_at resolved with an origin_device tiebreak so both phones reach the same answer, never deletes, all-or-nothing on a dangling reference, losing versions written whole to conflict_log, and Backup.restore reidentifies the device (Merge.kt:145-212, MergeApply.kt:65-166, Backup.kt:559).
WORKED-WELL | Rule 2 holds under real pressure on Progress and Measure: no range, threshold, color-by-value, arrow or trend, and spread() states in code that the person's own min and max are not a reference range (ProgressScreen.kt:355-370, 445-448).
WORKED-WELL | Rule 17 works where it was built: the entry's date is a control on the entry itself, opens the same picker, prefilled, 4 taps from the trail (EntryScreen.kt:144, DatePicker.kt:110).
WORKED-WELL | The bin and tombstone discipline: delete is always a tombstone, restorable from More, and the bin query probes pragma_table_info for subject_id (Repository.kt:1223-1245). The prior_auth_appeal template is genuinely strong: 4 stages, 12 steps, 3 date kinds, 6 paper places, 5 roles.
