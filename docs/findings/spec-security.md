FINDING | HIGH | import | ExportContainer.kt:1113-1118 | Attachment entry names from the inner archive are joined to the staging directory with no traversal check, and the write happens before any validation. requireSafeName (:539) is called only on the WRITE path (:489). An entry named attachments/../../shared_prefs/health_trail_key.xml becomes File(staging, "../../shared_prefs/...") and out.parentFile?.mkdirs(); out.writeBytes(bytes) overwrites it. Checksum and hash checks run afterward (:1155, :1216), so the file is already on disk. Destroying the wrapped passphrase makes the notebook permanently unopenable (DatabaseKey.passphrase() throws DatabaseKeyLost) | Resolve the target and require out.canonicalPath.startsWith(staging.canonicalFile.path + File.separator); reject any name containing ".." or a leading "/" | safe
  MAIN-SESSION CORRECTION, verified 2026-08-18: requireSafeName does NOT reject "..". It checks ASCII 32-126, length <= 180, the characters <>:"|?*\ , and reserved device stems. The string ../../shared_prefs/health_trail_key.xml passes every one of those. Reusing requireSafeName on read, which the panel also suggested, does NOT close this. Only the canonical containment check does.
  MAIN-SESSION SEVERITY NOTE: no network anywhere in the app, so this needs the person to be handed a malicious archive AND the attacker's passphrase. Realistic in the stated threat model, not remote. High, not critical.
FINDING | HIGH | injection | Backup.kt:440-441 (also :403, :463) | View names and view column names taken from a restored archive are interpolated unquoted into SQL, because the import validator whitelists tables and columns but never views. ExportContainer.unknownShape (:1379-1382) enumerates only sqlite_master WHERE type = 'table'. encryptedCopy (Backup.kt:347) uses sqlcipher_export, which copies views and triggers too, so the archive's views land in the live database. restore then calls recomputeRanges -> edtfGroups -> userTables, which is type = 'view' AND name LIKE 'live_%' with removePrefix("live_"), and the result goes into "SELECT id, ${group}_edtf, ${group}_zone FROM $table WHERE ..." with rawQuery(..., null), no bound arguments. The archive's passphrase, manifest and databaseSha256 are all attacker-produced, so every integrity check passes by construction. UNVERIFIED: no proof-of-concept built; SQLCipher compiles one statement per call so chained DROP is unlikely; realistic impact is between a crash mid-restore and single-statement rewriting | Extend unknownShape to check type IN ('view','trigger') against the expected set, and quote identifiers consistently in Backup.kt 403/440/463 | needs-care: every legitimate archive carries the live_* views, so the check must whitelist the expected view set, not reject views outright, or all restores break
FINDING | MEDIUM | import | ExportContainer.kt:1102-1130, :962-976 | No entry-count, per-entry size, total-size or compression-ratio limit anywhere on the read path. The outer zip's payload.enc is streamed to cache with an unbounded copyTo (:967) before anything is authenticated, so a 1 MB deflate bomb fills storage on a file merely previewed. Inside, zip.readBytes() (:1106) pulls each entry fully into heap, so one entry declaring 4 GB decompressed is an OOM kill mid-restore. MAX_FRAME_BYTES (:1310) caps only the encrypted frames, not the zip layers | Cap total decompressed bytes and entry count on both zips; stream DATABASE and attachments to disk instead of readBytes() | safe
FINDING | MEDIUM | leakage | RestoreFlow.kt:106,144 with ExportContainer.open() | The restore staging directory is never deleted, leaving a PLAINTEXT SQLite copy of the entire care record in cache indefinitely. open() deletes payload.enc and payload.zip but not staging/trail.sqlite or the extracted attachments, and restore-source.htx (RestoreFlow.kt:60) is never removed. The payload is plain unencrypted SQLite by design (contract 8.1), so after one restore, or one FAILED restore, the whole notebook sits readable at cacheDir/restore-staging/trail.sqlite. That defeats the SQLCipher-at-rest guarantee against anyone who can read app storage. Backup.decryptedCopy on the export side does clean up (Backup.kt:245) | staging.deleteRecursively() in a finally on every exit path, after apply rather than inside open(); delete restore-source.htx when the flow leaves | needs-care: Opened hands database and attachments to the caller
FINDING | MEDIUM | leakage | MainActivity.kt (absent) | Nothing sets FLAG_SECURE or disables the recents thumbnail; grep for FLAG_SECURE/setRecentsScreenshotEnabled returns nothing across main. On a shared device the recents screen shows the last frame: an emergency card, a diagnosis, a medication list, and the export passphrase field is screenshottable. The stated threat model includes a shared device and a curious family member | setRecentsScreenshotEnabled(false) app-wide (API 33+), and FLAG_SECURE on the export and restore passphrase screens at minimum | needs-care: blanket FLAG_SECURE also blocks the person's own screenshots of their record, which rule 23 argues against; recents-only is the safe subset
FINDING | LOW | files | NotebookShell.kt:3259-3263 | The 25 MB attachment cap is skipped when the content provider reports unknown length: openAssetFileDescriptor(...).length returns -1 for many providers, so if (size > MAX_ATTACHMENT_BYTES) return null passes and Attachments.put(stream) streams unbounded to disk. Only reachable through PickVisualMedia, so a disk-fill nuisance rather than a compromise | Count bytes during the stream copy and abort past the cap | safe
FINDING | LOW | crypto | ShellState.kt:545, ExportScreen.kt:96, RestoreScreen.kt:130 | The export passphrase is held as a Kotlin String in Compose state, which cannot be zeroed. ExportCrypto.derive correctly takes a CharArray and every caller wipes it, but chosen.toCharArray() (NotebookShell.kt:1102) copies from a String that lingers in heap until GC. It is mutableStateOf, not rememberSaveable, so it does NOT reach a bundle or disk, verified | Carry a CharArray through the screens, or accept and document | needs-care: BasicTextField is String-based
FINDING | hardening | import | ExportContainer.kt:951 | expected: Schema? = null makes the only schema whitelist opt-in and off by default. Both production call sites do pass it, so nothing live is exposed; a future caller that forgets silently loses the table/column gate that also closes the MergeApply.kt:237,252 column interpolation | Make the parameter required | safe

CLEAN, do not "fix" these:
Argon2id + AES-256-GCM (ExportCrypto.kt): 64 MiB / 3 passes, above OWASP baseline, parameters read from the file on import so old archives still open, 128-bit tags, random 16-byte salt per export, SecureRandom throughout. No hand-rolled primitives.
Frame nonces and AAD (ExportCrypto.kt:129-148, ExportContainer.kt:1256-1302): random per-file prefix plus a 64-bit counter, never random per frame; index and finality bound into the tag; GCM verifies before returning plaintext, so the container authenticates before it decrypts. MAX_FRAME_BYTES caps allocation from a length prefix.
DatabaseKey.kt: 32 random bytes wrapped by a non-exportable AndroidKeystore AES-GCM key, only wrapped bytes in prefs, commit = true, key fill(0) after SQLCipher takes it. Refusing to regenerate on unwrap failure is correct.
Repository SQL: 123 SQL sites, zero unsafe value concatenations. Every interpolated identifier comes from a fixed set (the Section enum, dateColumns literal bases, ABOUT_HEADINGS, if/else operators); $marks is always joinToString { "?" }; search escapes LIKE metacharacters and binds the pattern (Repository.kt:4653-4689).
Manifest and build: allowBackup=false, dataExtractionRules excludes all five domains for both cloud-backup and device-transfer, no INTERNET and no permissions at all, one exported LAUNCHER activity with no deep links, FileProvider exported=false scoped to cache/shared/ only, release isMinifyEnabled/isShrinkResources true with correct SQLCipher and BouncyCastle keep rules, keystore properties gitignored.
Logging: exactly one Log. call in the whole shipping tree (NotebookShell.kt:1347, a throwable with no interpolated data), no println, no printStackTrace. Share.kt keeps the person's name out of the subject line, clears old shares before writing, and grants read via clipData + FLAG_GRANT_READ_URI_PERMISSION only.
