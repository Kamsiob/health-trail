WALK1 | critical | cannot-file | 5 taps | Photographing the insurance card and tapping Save without typing a title: the Save button has no `enabled` guard (AddDocumentScreen.kt:337), but the handler's guard is `else if (subject != null && title.isNotBlank())`, so a blank title falls to a bare `else { addingDocument = false }` that closes the screen and discards the stored photo with no write and no error | ui/NotebookShell.kt:2368
WALK1 | high | dead-end | Only launcher in main source is PickVisualMedia, gallery-only, so every paper requires leaving to the camera app first | AddDocumentScreen.kt:265
WALK1 | high | cannot-file | The portal discharge summary is a PDF; the picker is ImageOnly | AddDocumentScreen.kt:434
WALK1 | high | cannot-file | No date-of-birth field in setup, in createSubject, or in the correction screen | SetupScreen.kt:164, Repository.kt:185
WALK1 | high | wrong-place | 4 taps | Member ID and plan have one home in the whole app: a free-text insurance_note box on the emergency card, reached by scrolling past the resuscitation and DNR questions on admission night | EmergencyCardEditScreen.kt:281, schema.sql:1222
WALK1 | medium | tap-cost | ~55 taps, 10 app exits | Ten papers: FAB, Document, sheet, picker, title, Save each; no save-and-add-another anywhere | AddDocumentScreen.kt:331
WALK2 | critical | cannot-file | Typing an answer to an open question on the prep sheet sets both savingAnswer and markingAsked in one handler; two independent LaunchedEffects race, and markQuestionAsked defaults answerText to null and unconditionally writes answer_text = NULL, so the answer just typed is erased if it lands second | NotebookShell.kt:1962 and :1967, null write at Repository.kt:4442
WALK2 | high | cannot-file | 9 taps | CaptureFormState holds thread, person, medication and project but no appointment, so the appointment must be created cold afterward and the call and appointment stay unlinked | CaptureFormScreen.kt:122
WALK2 | high | missing-link | A question cannot be attached to Tuesday's appointment: asked_at_appointment_id records only where a question WAS asked, past tense; no forward binding, so the prep sheet shows all open questions rather than that appointment's | Repository.kt:4426, PrepScreen.kt:220
WALK2 | high | missing-link | "Write it up" opens a bare CaptureKind.VISIT with nothing carried; the visit note never links to the appointment and the app never learns Tuesday happened | NotebookOverlays.kt:139
WALK2 | high | dead-end | Acting on what was decided: the visit note offers Correct, Pin, Remove only. No change-the-dose, no follow-up appointment, no standing instruction | EntryScreen.kt:357
WALK2 | medium | cannot-retrieve | The prep sheet carries questions, changes and memos but no current medication list | Repository.kt:5482
WALK3 | critical | cannot-file | `isUnfiled = threads.isNotEmpty() && draft.threadId == null`, so on any notebook without pre-existing care threads nothing ever reaches the unfiled tray | NotebookShell.kt:3064
WALK3 | critical | cannot-file | fileEntry writes entry_thread or nothing, so the tray's entire destination set is {one care thread, none-of-these} | Repository.kt:1491
WALK3 | high | wrong-place | `rough` defaults to RoughWhen.TODAY and WHEN is stage 3, so a fast save stamps "mum fell on Saturday" with today's date and the person is never asked | CaptureFormScreen.kt:125
WALK3 | high | tap-cost | 4 to keyboard, 15 for three facts | "Log a call" opens on the WHO stage, so the note field needs a Skip first and no field requests focus; one capture carries one note | CaptureFormScreen.kt:1131
WALK3 | high | cannot-file | Filing clears is_unfiled in the same transaction as the single link, so one note can never be filed to a second place, and there is no split action | Repository.kt:1505
WALK4 | critical | cannot-file | The letter cannot be a bill and a document at once: bill has no document_id and attachment sets exactly one owner | schema.sql:840, :817
WALK4 | critical | missing-link | filingsForDocument reads only project_paper, so a document can never say it is a bill and a bill can never point at its photo; rule 18 unmet on the money axis | Repository.kt:7267
WALK4 | high | cannot-file | Three pages means three documents: single-image PickVisualMedia, one picked Uri, one sha256, no function adds a second attachment | AddDocumentScreen.kt:266
WALK4 | high | cannot-file | 0 taps exist | From the document, filings render read-only with onOpenProject only; the appeal must be entered from the project side | DocumentScreen.kt:283
WALK4 | high | missing-link | No path from the letter to warfarin: document has chapter, entry and project columns but no medication link, and the generic link table is only ever written with source_table='entry' | schema.sql:769
WALK4 | high | dead-end | 3 taps lost | Photographing from an empty project paper slot clears fillingPaper and jumps to capture; the document saves unattached and the slot is still empty | NotebookOverlays.kt:1683
WALK4 | medium | wrong-place | updateMedication writes dose_text directly with no medication_event, so editing the dose from Edit erases the old one; only the separate "Record change" action preserves history | Repository.kt:611
WALK5 | critical | cannot-retrieve | "When did the confusion start" is unanswerable: search is ORDER BY created_at DESC LIMIT 50 per section, so with 900 entries the first mention is absent, and nothing on screen says it truncated | Repository.kt:4711
WALK5 | critical | cannot-retrieve | No sort control and no oldest-first anywhere; ordering is by when it was typed, not when it happened. SearchHit.occurredStart is populated and never used to sort | Repository.kt:4617
WALK5 | critical | cannot-file | 0 taps exist | Six old entries cannot join a new care thread: linkEntryToThread has exactly one production caller, inside the capture draft | Repository.kt:1679, sole caller NotebookShell.kt:3067
WALK5 | high | wrong-place | The screen labels its first row "Best match", but results reach it unsorted, in fixed notebook-section order. No ranking exists | SearchScreen.kt:214 vs Repository.kt:4743
WALK5 | high | cannot-retrieve | Search covers 10 tables and excludes measurement value_text and note, incident description and resolution_note, care-thread labels, medication events and project steps, while its empty state claims "It looks through every section at once". Every categorical, event-log and photo-log tracked value lives in value_text and is invisible | Repository.kt:4743, contract/i18n/en.json:1314
WALK5 | high | tap-cost | ~8 taps plus arithmetic | "How many falls since March": the trail filters by kind but has no date range | TrailScreen.kt:385
WALK5 | high | missing-link | Handing the phone to the neurologist shows rename, remove and correct controls; there is no read-only view | OneThread.kt:142, EntryScreen.kt:101
WALK5 | medium | wrong-place | Section counts are off by one: results.drop(1) strips the lead hit from its own group's tally | SearchScreen.kt:231
WORKED-WELL | Dictation is real and rule-bound: ACTION_RECOGNIZE_SPEECH with the rule centralized in DictatableField so number, phone and passphrase fields get no microphone across all 34 call sites (Fields.kt:276).
WORKED-WELL | Capture Save is live from stage one and drafts survive back, rotation and process death via CaptureFormState.Saver; filing touches only is_unfiled and the link, so original words, occurred_edtf and created_at survive.
WORKED-WELL | The situation template genuinely lands: "Hospital stay" creates 4 threads, 10 first-day steps, 5 paper slots and a Today layout (Repository.kt:729), and setup and situation are both skippable without a dead end.
WORKED-WELL | Prep share is real (Readable.prep to a chooser with a graceful no-handler path), project_paper is a true join table, medication_event has a working dose_changed writer, and bill holds minor-units amount, EDTF due date and a disputed state.
