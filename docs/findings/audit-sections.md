FINDING | high | hierarchy | EmergencyCardScreen.kt:410-414 | Every CardField value renders at displayS, so a filled card shows up to eight display-size headlines with nothing leading | Give the one field the person opened this screen for the lead treatment, drop the rest to rowTitle.
FINDING | high | hierarchy | ui/v4/Surfaces.kt:244 | The Lead composable is called by 0 of 27 screens; Milestones, Incidents, Appointments and Standing instructions render N items at one weight, and StandingInstructionsScreen.kt:170-181 computes a lead and spends it only on sort order | Pass a lead flag through the row composable on those four, as CareThreadsScreen.kt:165 already does.
FINDING | high | state | ChapterScreen.kt:159-185, :302 | Milestone and document cards pass onTap = null while still capping body at maxLines = 3 with an ellipsis, so text is truncated with no way to read the rest, and they look identical to the tappable incident cards beside them | Make them doors, or remove the cap.
FINDING | high | motion | ui/v4/People.kt:294, ChaptersScreen.kt:258, ChapterScreen.kt:285, MilestonesScreen.kt:166, CareThreadsScreen.kt:218, PersonScreen.kt:305 | Six sites apply Modifier.clickable to the modifier handed into Block, which lands outside Card's clip, so the ripple draws square over a 26dp corner, the exact failure Press.kt:52-56 documents | Add a Block(onClick=) overload routing through opensOnTap(shape = Radius.cardLarge).
FINDING | high | component | AppointmentsScreen.kt:53/244, EmergencyCardScreen.kt:41/178, MonthReviewScreen.kt:42/277, PrepScreen.kt:49-54 | Four screens carry *_FOLD test tags on a plain Eyebrow and doc prose describing a count, with the fold removed and dead rememberSaveable/mutableStateOf imports still present; every past appointment, medication and entry renders unconditionally | Restore Fold (ui/v4/Fields.kt:343) with its count on all four.
FINDING | high | consistency | MedicationList.kt:103-193, OneThread.kt:120-183 | Both hand-roll Scaffold + LargeFlexibleTopAppBar instead of Page, losing the subtitle, the tips lamp, the edit pencil and .arrives(ARRIVAL_LIST); OneThread also spaces its list at Space.cardGap (10dp) against every other screen's 24dp | Rebuild both on Page.
FINDING | medium | state | OneThread.kt:174, :214, :228-231 | An empty thread states the same fact three times at three weights, which the file's own doc at :218-224 claims to have fixed | Keep one, as SectionEmpty.
FINDING | medium | copy | ChaptersScreen.kt:143 | The eyebrow chapters.current = "Where they are now" heads a road containing current + earlier (:98), so it labels every place the person has left | Split the two groups, or relabel.
FINDING | medium | spacing | ui/v4/Rows.kt:273 vs EmergencyCardScreen.kt:378, MedicationList.kt:270, QuestionsScreen.kt:241, BinScreen.kt:97 | Four divider insets in one app: the shared 56dp; a hand-built 44dp; inset = false on rows that do have a 44dp mark so the hairline cuts the tile; and 56dp over a 24dp markCard, a 16dp overhang past the text | Derive RowDivider's inset from the caller's markSize.
FINDING | medium | color | MonthReviewScreen.kt:118, SearchScreen.kt:110, IncidentScreen.kt:93/:335 | These never call hueFor; Month review passes section = null and renders four markless gray ListRow groups for four different sections, Search greys its section-named eyebrows, Incidents draws a gold eyebrow over a screen of colors.alert | Pass section = and use entryHue/hueFor on the marks.
FINDING | medium | color | ChaptersScreen.kt:256/276, CareThreadsScreen.kt:214 | The section hue marks current-vs-past and lead-vs-not, i.e. state rather than identity, which Surfaces.kt:158-161 forbids | Keep the hue on every item and carry state in weight or alpha.
FINDING | medium | state | SearchScreen.kt:190 | EmptyDrawing(section = null) returns without drawing (EmptyDrawing.kt:59), followed by Spacer(Space.l), so the no-match state opens with 24dp under an invisible component; :134 also passes fillParentMaxHeight(0.5f), which SectionParts.kt:170-174 documents as removed | Use SectionEmpty for all three of this screen's empty states.
FINDING | medium | state | CareTeamScreen.kt:332, MilestonesScreen.kt:144 | Both emit their list container unguarded, so a one-person team and an empty Milestones screen each draw an empty bordered Block under the content | Guard on isNotEmpty().
FINDING | medium | color | PersonScreen.kt:274, ChaptersScreen.kt:216 | Appointment rows on a person's screen and the milestones door on Chapters both wear goldHue() instead of their own section's slate and chapters hue | Use hueFor for the destination.
FINDING | medium | state | MoneyScreen.kt:96-97 | amountMinor is summed across every open bill regardless of currency and labeled with the first bill's currency, so a EUR400 and a USD400 bill total "$800" | Total per currency, or suppress the total when currencies differ.
PATTERN | Nine vertical gap tokens in use (4, 8, 10, 12, 16, 20, 24, 32dp) across 74 explicit Spacers; the eyebrow-to-content gap alone is drawn at 4, 8, 10, 24 and 32dp; six lazy items exist whose only content is a spacer, each also absorbing two 24dp itemSpacing gaps.
PATTERN | labeledBlock (ui/v4/Page.kt:486) is reimplemented by hand on 14 sites across 8 screens; its `leading` parameter is declared and never read, so "one colored group per screen" is unimplemented everywhere.
PATTERN | The "date eyebrow, title, three-line body" card exists as 7 separate private composables (Chapter, OneThread, Prep, Incident x2, MonthReview, Search, Person) that already disagree on truncation and untitled fallback.
PATTERN | Five of eight section screens pass eyebrowColor instead of section to Page and silently lose the tips lamp; three empty-state treatments coexist, which SectionParts.kt:139-143 argued was already fixed.
PATTERN | Grouped lists name their SECONDARY group and leave the primary one unlabeled on 4 screens (Appointments, Medications, Care threads, One thread).
CONTESTED | The PM held the six ripple-clip sites cosmetic; the UI designer noted it is the one defect visible on every single tap of a card, and it won.
CONTESTED | The researcher wanted the absent scoped search and scrubber filed per screen; the PM read it as feature work, so it is out.
CONTESTED | The director wanted the ~40 dead imports and parameters listed; all four agreed a user cannot see them, and they were cut.
