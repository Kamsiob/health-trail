PART-A obs1 | One header, one component: promote Page.kt's bar into a `PageBar` composable (LargeFlexibleTopAppBar + exitUntilCollapsedScrollBehavior, containerColor=background, scrolledContainerColor=surfaceContainer, eyebrow in the subtitle slot) and make it the only place in the app that draws a screen title. ProjectsScreen gets a real Scaffold + PageBar, its title and HeaderActions leave LazyColumn item 0. TodayField, Notebook, OneThread, MedicationList delete their own bars and pass extras through PageBar's actions slot. TodayScreen.Masthead deleted with its fallback path. Setup/Disclaimer/SituationPicker/CorrectSubject keep their Column body but gain PageBar, so displayL-in-a-Column disappears.
TOUCHES 12 files: ui/v4/Page.kt, ui/v4/HeaderAction.kt, screens/TodayField.kt, Notebook.kt, OneThread.kt, MedicationList.kt, ProjectsScreen.kt, TodayScreen.kt, SetupScreen.kt, DisclaimerScreen.kt, SituationPickerScreen.kt, CorrectSubjectScreen.kt
BREAKS: ProjectsScreen LazyColumn indices shift by one so every arrives(index) stagger and index-based instrumented assertion moves; the four onboarding screens gain a status-bar-inset bar they were never drawn with (rule 12: screenshot, needs-design-review, DESIGN.md 14, HANDOFF.md); Setup's displayL title shrinks to the bar's collapsed size.

PART-A obs2 | Cause is structural: LargeFlexibleTopAppBar pins `actions` to the top row and puts the large title on the second, so on a short screen the bar parks mid-collapse with the title traveled and the lamp not. Fix: move HeaderActions out of `actions` into the `title` slot as the trailing element of Row(fillMaxWidth, SpaceBetween, CenterVertically), leaving `actions` for the badge only. Material then collapses title and lamp as one row. HeaderActions needs an explicit tint since it now inherits titleContentColor; HeldSlot (HeaderAction.kt:107) stays. Fallback inside the same PageBar: keep `actions` and drive the row's graphicsLayer scale and translationY from scrollBehavior.state.collapsedFraction.
TOUCHES 2 files plus the 5 bars folded in by obs1.
BREAKS: collapsed bar height grows to the 48dp IconButton target so content start moves down a few dp on every screen; TalkBack order changes from title-then-actions to title, lamp, badge; HeaderActionTags.ROW moves inside the title node.

PART-A obs3 | One contract, enforced structurally: Page takes the screen's way in as a single `PageAction(label, mark, onTap)`, renders it as the scaffold FAB (D200), and publishes it through a `LocalPageAction` that SectionEmpty (SectionParts.kt:167) reads as the default for actionLabel/onAction. A screen therefore cannot have a FAB and an actionless empty card, and the button is always the quiet ActionEmphasis control INSIDE the Block; a screen with genuinely no action passes null, the honest state for Bin and Search. `lead` becomes required wherever the screen has a section, so mark, lead, sentence and action are one fixed shape across all 23 sites.
TOUCHES 11 files plus the four string catalogs.
BREAKS: six empty states gain a button with no copy, so all four catalogs need keys or the parity check fails; MonthReview and ProjectTrail have no obvious single action; the six screens' return@Page early exits must move.

FINDING | high | navigation | NotebookShell.kt:449-530 | ~40 detail destinations are a flat set of nullable state flags rendered by bare `if`, so every list-row-to-detail push and back pop is a hard cut; the one animated transition is destination-to-destination at line 641 | Derive a single topOfStack from the flags and wrap the body in one AnimatedContent using the forward/back pattern already at line 641.
FINDING | high | motion | NotebookShell.kt:2835/2854 | The FAB opens CaptureBloom with the reserved expressive() rise (CaptureBloom.kt:206), then choosing a kind cuts instantly to a full-screen form; the flow the app is built around loses its motion at the moment it matters | Include the capture branch in the same AnimatedContent with scaleIn(expressive(), transformOrigin = bottom-end) + fadeIn(quick()).
FINDING | high | press | ui/components/Press.kt:68-127,222,230 vs ui/v4/Press.kt:88 | Two complete press systems coexist: the legacy one lerps resting color 8% toward ink, springs a scale, hand-animates a focus ring, passes indication=null; the v4 one uses Material's ripple(). Rule 16 requires one treatment | Delete ui/components/Press.kt and migrate callers to Surface(onClick=) or opensOnTap; decide once whether pressScale/focusRing survive.
FINDING | medium | press | components/Confirm.kt:156, components/Tile.kt:105, components/StepRow.kt:88, ProjectDetailScreen.kt:513, CaptureBloom.kt:130, ProjectTrailScreen.kt:256 | Six live indication=null clickables remain after the D196 sweep | Pass indication = ripple() with the caller's own MutableInteractionSource, or convert to Surface(onClick=).
FINDING | medium | reduced-motion | ui/theme/Motion.kt:254-264 | rememberSystemMotion is @ReadOnlyComposable and reads ANIMATOR_DURATION_SCALE once with no observer, so turning reduced motion on while the app is open leaves FullMotion running until process restart, which is exactly how rule 19 is verified | Back it with a ContentObserver on that Global URI inside produceState.
FINDING | medium | motion | ui/v4/BottomNav.kt:218-224 | NavMark flips color between ink and Transparent with no animation, so the selected tile cuts while the page beside it slides | animateColorAsState(motion.standard()) on color and contentColor.
FINDING | medium | motion | ui/v4/Sheet.kt:57, ui/theme/Motion.kt:95 | settle() is documented as the spring for a sheet and has zero sheet callers; every sheet runs Material's default, so the token asserts behavior nothing implements | Either wire the sheet spec through MotionScheme at the theme or apply settle(); do not leave the claim.
FINDING | medium | consistency | ui/v4/HeaderAction.kt:78 | The early return when all three actions are null means some screens have no action row and others reserve a 48dp HeldSlot, so the title's width and truncation point differ screen to screen | Always emit the Row with HeldSlot within a screen family.
FINDING | low | motion | ui/v4/Arrival.kt:49 | Modifier.composed is deprecated, and LaunchedEffect(Unit) re-fires when the page recomposes into a new AnimatedContent slot, so once obs1/obs2 land every detail push replays the arrival stagger on top of the push | Convert to a Modifier.Node factory and gate arrives off while an enclosing transition runs.
FINDING | low | consistency | ui/v4/Page.kt:239 and six sibling bars | Each constructs its own exitUntilCollapsedScrollBehavior(), so collapse distance and settle differ per screen | One PageBar owns it; add TopAppBarDefaults to the banned-pattern check in tools/checks/.

CONTESTED | Motion designer wants obs2 solved by moving HeaderActions into the title slot; systems lead objects that this puts interactive controls in a text slot, fights titleContentColor, and grows the collapsed bar to 48dp everywhere, preferring the collapsedFraction fallback. Researcher breaks the tie toward the title slot: it is the only option where the alignment cannot drift again.
CONTESTED | Systems lead wants ui/components/Press.kt deleted outright; motion designer argues the 0.985 press scale and focus ring are real value ripple does not replace, and would fold pressScale into opensOnTap. Unresolved, owner's call because it is visible.
CONTESTED | Researcher argues MonthReview and ProjectTrail should stay actionless, since inventing an action to satisfy the contract is decoration; systems lead argues an empty screen with a FAB and no in-card route is the exact defect reported. Compromise: those two pass null and lose the FAB too, so the screen is consistently actionless rather than half.
