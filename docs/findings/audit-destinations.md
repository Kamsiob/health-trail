FINDING | medium | motion | ui/v4/Page.kt:346 | PullToRefreshBox wraps every page but onRefresh has zero callers app-wide, so a pull on any of the 62 Page screens draws Material's refresh indicator and does nothing | Compose PullToRefreshBox only when onRefresh != null.
FINDING | medium | motion | TodayField.kt:613 and Notebook.kt:275 | Today and Notebook are the only two lists with no .arrives(), so the front door and the table of contents cut into place while Projects, Notes and More settle | Add .arrives(ARRIVAL_LIST) as ProjectsScreen.kt:152 does.
FINDING | high | motion | NotebookShell.kt:2831 | The capture menu is a bare `if (sheetOpen)`, so the 62% scrim paints in one frame and the menu vanishes in one frame while the gold button spends a spring rotating back; DESIGN.md 15 says sheets rise, never appear | AnimatedVisibility(visible = sheetOpen, enter = fadeIn(quick()), exit = fadeOut(quick())) and hold sheetOpen until the exit completes.
FINDING | medium | motion | CaptureBloom.kt:112 | The stagger loop delays BLOOM_STEP_MILLIS per choice unconditionally and the motion read at line 98 is never consulted, so with reduced motion on the seven choices still arrive over ~180ms, against rule 19 and the file's own comment | Delay if (motion.isReduced) 0L else BLOOM_STEP_MILLIS.
FINDING | high | motion | CaptureFormScreen.kt:444,469,525 | Each stage of the staged conversation is a bare `if` in a Column, so Next and Back replace the question in one frame with no transition, on the flow the app is opened for | Wrap the three stage blocks in one AnimatedContent(targetState = stage) using the shared-axis spec at NotebookShell.kt:641.
FINDING | medium | hierarchy | MoreScreen.kt:263 | Arrangement.spacedBy(Space.sm) puts 12dp above and below every group eyebrow, so each label sits equidistant from the block it names and the block above it and names neither | spacedBy(Space.none) plus Spacer(betweenGroups) above each eyebrow and Spacer(withinGroup) below, as ProjectsScreen.kt:261 and NotesScreen.kt:246 already do.
FINDING | medium | color | NotesScreen.kt:372 | A note card carries no mark at all, so a full Notes screen is neutral cards on neutral canvas with the only color the pin tint, while every other list wears a saturated hue disc per D198 | HueMark(hueFor(NOTES), Symbols.of(NOTES), Space.markInline) at the head of the card.
FINDING | medium | spacing | NotesScreen.kt:213,224,247,305,325 | The screen pays Page's default itemSpacing = betweenGroups and adds its own spacers, so the search-to-toggle gap is 48dp and two note cards sit 32dp apart in a list meant to be scanned | Pass itemSpacing = Space.none and drop the per-card trailing Spacer to withinGroup.
FINDING | medium | state | NotesScreen.kt:382 | semantics(mergeDescendants = true) folds the pin and remove buttons into the card's single node, so the two controls the owner asked for on the list are unreachable with the reader on | Use TodayField.kt:1276's pattern: plain semantics on the card, clearAndSetSemantics on the text column, IconButtons outside the silence.
FINDING | low | motion | ui/v4/BottomNav.kt:222 | The selected tile is an unanimated Surface color swap, so the mark snaps while the page behind it slides | animateColorAsState with HealthTrail.motion.standard().
FINDING | low | spacing | AppearanceScreen.kt:106,123 | item { header() } and item { footer() } are emitted even when both lambdas are empty, so Appearance on its own carries two 24dp dead items | Make the parameters nullable and guard with ?.let.
CONTESTED | The PM wanted the inert pull gesture wired to a real re-read since a restore genuinely changes what is on disk; the design director held that an affordance nobody asked for and nothing calls should go.
CONTESTED | The UX researcher read the Notebook as failing law 1, twelve rows of one weight with no lead; the design director held that D163 made it a flat table of contents deliberately and a manufactured lead would be the app guessing what matters.
CONTESTED | The UI designer wanted a container transform from a note card into NoteScreen; the PM refused, because the shell routes by state in a `when` rather than a navigation graph.
