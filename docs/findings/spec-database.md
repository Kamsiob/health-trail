1 | high | transaction | Repository.kt:236-247 makeSubjectActive | Two UPDATE subject statements with no transaction, though the comment above says "The clear and the set are one transaction for that reason" | Process death between them leaves ZERO active subjects; every subject-scoped screen reads the active subject, so the notebook opens empty and the person believes their records are gone | Wrap both writes in beginTransaction/setTransactionSuccessful/finally endTransaction | safe
2 | high | transaction | Repository.kt:5952-6009 recordMedicationEvent | Inserts medication_event then up to two separate UPDATE medication each in its own withContext, no transaction; its comment claims "The event and its consequence are one transaction" | A stop event is logged but the medication is never marked stopped, so the med list keeps showing a drug the person stopped and the trail says otherwise | One transaction around the insert and both updates | safe
3 | high | transaction | Repository.kt:3873-3891 moveToChapter | Ends the open chapter with an UPDATE then insertRow("chapter"), unwrapped | A failure between them leaves no open chapter: new entries file nowhere and the spine shows a gap that cannot be repaired from the UI | Wrap the pair | safe
4 | high | contract | Backup.kt:436-470 recomputeRanges | One UPDATE per row without touching updated_at or rev | Each update fires the change log trigger, so a restored five-year notebook appends thousands of change_log rows carrying the row's OLD rev and OLD updated_at; a peer asking "everything after seq N" gets changes it cannot order or dedupe, the silent sync/merge break rule 3 guards. Restore is also slow | Suppress logging for index rebuilds (owner call) or batch into a single UPDATE per group; do NOT bump rev, that would falsify user revisions | needs-care
5 | high | transaction | Backup.kt:491,496,514 restore | The pre-restore safety copy live.copyTo(backup) copies only health-trail.db; -wal and -shm are excluded and then deleted at :514 | If the connection is in WAL mode the safety copy is missing every commit still in the WAL, so a restore that fails and rolls back hands back a notebook silently short of the most recent entries | PRAGMA wal_checkpoint(TRUNCATE) before copying, or copy -wal/-shm alongside | needs-care
6 | medium | migration | HealthTrailDatabase.kt:149-163, Migrations.kt:144 | applySchema routes PRAGMA journal_mode = WAL through rawQuery INSIDE beginTransaction(); SQLite refuses a WAL switch inside a transaction, and the migration path filters PRAGMAs out entirely | The journal mode the contract declares is never applied; the effective mode is whatever the driver defaults to. UNVERIFIED which mode is live on device, and that determines whether finding 5 bites | Apply journal_mode outside any transaction in create(), next to the foreign_keys line | safe
7 | medium | integrity | HealthTrailDatabase.kt:106 | PRAGMA foreign_keys = ON is a single execSQL on a pooled SQLiteDatabase; the pragma is per-connection and the driver keeps a pool | UNVERIFIED: if a write lands on a connection that never got the pragma, orphan rows become insertable and no test would catch it | Assert at runtime after open, or use the pool-wide FK setter | safe
8 | medium | contract | Repository.kt:1330-1335 clearEveryLeadForTest | UPDATE today_card SET is_lead = 0 with no updated_at and no rev bump; ships in the MAIN source set | Fires the trigger and writes a change_log row at a stale rev, so the Today lead slot merges wrong across devices | Add updated_at and rev, or move to androidTest | safe
9 | medium | transaction | Repository.kt:520-573 createPerson; 673-803 applySituation | Multi-row sequences with no shared transaction | A half-applied situation template leaves a subject stamped with a template whose threads are missing, and no path re-runs it | Wrap each in one transaction | safe
10 | medium | transaction | Repository.kt:1093,1493,2760,3303,4378,4830,6799,8574 | Eight of fifteen transaction blocks call suspend insertRow/db() between begin and end; safe today only because withContext(Dispatchers.IO) from IO does not dispatch and Android transactions are thread-bound | If any helper ever gains a real suspension point, the write silently lands outside the transaction it believes it is in | Add a non-suspend insertRow(database, ...) overload for in-transaction callers | safe
11 | medium | index | contract/schema.sql | 45 FK columns have no leading index, including document.entry_id, measurement.entry_id, medication_event.entry_id, question.entry_id, attachment.measurement_id, project_person.person_id | At the five-year fixture each entry detail open scans these child tables end to end | Add covering indexes | SCHEMA-owner-call
12 | medium | integrity | MergeApply.kt:84,176-197 | readAll pulls every row of every mergeable table, both sides, into memory as strings before planning | A five-year notebook merge holds the whole database twice in heap; OOM on a mid-range phone kills the merge | Stream table by table | needs-care
13 | low | migration | Migrations.kt:106-123 | The v3 comment asserts SELECT * freezes a view's columns at creation; verified false on SQLite 3.53 | The false premise will mislead the next migration author | Correct the comment | safe
14 | low | migration | HealthTrailDatabase.kt:44-47, Migrations.kt:19 | SCHEMA_VERSION = 3 but its comment says "2"; the Migrations header says "Version 1 is the baseline" | Doc drift on the one number a bad migration turns on | Update both comments | safe

CLEAN
No DELETE FROM anywhere in the main sources; every removal is a tombstone.
The change log cannot be skipped: trigger-enforced in schema.sql, not application discipline. check_schema.py confirms 40 user tables each with the six columns, a live view and both triggers. ChangeLogTransactionTest proves an abandoned transaction takes its log entry with it.
No base-table read outside a live view: check_live_views.py passes over 311 Kotlin files.
Ordering: check_query_ordering.py passes; 112 render/export queries all order, and the trail orders on coalesce(occurred_start, created_at).
subject_id: every query without a subject filter is keyed on a specific row id, which is already single-subject.
Merge and migration mechanics: match by id only, defer_foreign_keys inside the transaction, attachments written before it (content-addressed so a stray file is harmless), inserts and updates split so the log records real ops, a future-versioned database is refused rather than opened, and each migration step is its own transaction that rolls back cleanly.
